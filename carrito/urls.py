from django.urls import path
from . import views
"""
app_name = 'carrito'

urlpatterns = [
    path('agregar/<int:id_producto>', views.agregar_carrito, name='agregar'),
    path('ver_carrito/', views.ver_carrito, name='ver_carrito'),
      
]"""